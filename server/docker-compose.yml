version: '3.8'

services:
  vibe-server:
    build: .
    container_name: vibe-server
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - API_KEY=vibe-secret-key # Change this!
      - NODE_ENV=production
    volumes:
      - ./logs:/app/logs
